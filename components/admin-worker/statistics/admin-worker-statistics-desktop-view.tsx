"use client";

import { ManagerStatisticsDesktopView } from "@/components/manager/statistics/manager-statistics-desktop-view";

/** Desktop admin-worker statistics — тот же UI, что у manager (сохранено из page). */
export function AdminWorkerStatisticsDesktopView() {
  return <ManagerStatisticsDesktopView />;
}
